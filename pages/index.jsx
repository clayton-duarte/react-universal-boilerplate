import React from 'react';
import { Template, Test, Button } from '../src/components';

export default () => (
  <Template title="Test Page">
    <Button>
      <Test />
    </Button>
  </Template>
);
